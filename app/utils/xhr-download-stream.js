// Generated by CoffeeScript 1.6.3
(function() {
  var Buffer, ProgressStream, Q, XHRDownloadStream, downloadChunk, events, getFileSize, stream, util;

  Buffer = require("buffer").Buffer;

  events = require("events");

  stream = require("stream");

  util = require("util");

  Q = require("q");

  getFileSize = function(requestUrl, cb) {
    var deferred, fileSize, xhr;
    fileSize = void 0;
    xhr = new window.XMLHttpRequest();
    deferred = Q.defer();
    xhr.open("HEAD", requestUrl);
    xhr.onload = function(evt) {
      fileSize = Number(this.getResponseHeader('Content-Length'));
      if (fileSize == null) {
        return deferred.reject(new Error("Could not determine filesize for URL: " + requestUrl));
      } else {
        console.log('Downloading file of size', Math.round(fileSize / 1049000), 'MB from', requestUrl);
        return deferred.resolve(fileSize);
      }
    };
    xhr.onerror = function(evt) {
      return deferred.reject(new Error("Error determining filesize for URL: " + requestUrl));
    };
    xhr.send();
    return deferred.promise;
  };

  downloadChunk = function(requestUrl, start, end) {
    var deferred, xhr;
    xhr = new window.XMLHttpRequest();
    xhr.open("GET", requestUrl);
    xhr.responseType = "arraybuffer";
    xhr.setRequestHeader("range", "bytes=" + start + "-" + end);
    deferred = Q.defer();
    deferred.promise.cancel = function() {
      return xhr.abort();
    };
    xhr.onload = function() {
      nwGui.App.clearCache();
      if (this.status >= 200 && this.status <= 299) {
        return deferred.resolve(new Buffer(new window.Uint8Array(this.response)));
      } else if (this.status === 416) {
        return deferred.resolve(null);
      } else {
        return deferred.reject(new Error("Got status code: " + this.status + " for chunk."));
      }
    };
    xhr.onprogress = function(evt) {
      if (evt.lengthComputable) {
        return deferred.notify(evt.loaded);
      }
    };
    xhr.onerror = function(evt) {
      return deferred.reject(new Error("Error downloading chunk " + start + '-' + end));
    };
    xhr.send();
    return deferred.promise;
  };

  XHRDownloadStream = function(targetUrl, chunkSize) {
    var _this = this;
    stream.Readable.call(this);
    this._bytesSoFar = 0;
    this._fileSize = 0;
    this._chunkSize = chunkSize;
    this._targetUrl = targetUrl;
    this.progressStream = new ProgressStream();
    this.progressStream.setCanceller(function() {
      return _this.cancel();
    });
    getFileSize(targetUrl).then(function(fileSize) {
      return _this._fileSize = fileSize;
    });
    return null;
  };

  util.inherits(XHRDownloadStream, stream.Readable);

  XHRDownloadStream.prototype._read = function(size) {
    var chunkSize,
      _this = this;
    console.log(chunkSize = this._chunkSize || size);
    this.currentRequestPromise = downloadChunk(this._targetUrl, this._bytesSoFar, this._bytesSoFar + chunkSize);
    return this.currentRequestPromise.then(function(data) {
      if ((data == null) && _this._bytesSoFar !== _this._fileSize) {
        throw new Error("Expected file of size: " + _this._fileSize + " but got: " + _this._bytesSoFar);
      }
      _this._bytesSoFar += (data != null ? data.length : void 0) || 0;
      return _this.push(data);
    }, function(reason) {
      return _this.emit("error", reason);
    }, function(bytesLoadedByCurrentRequest) {
      var percentComplete;
      if (_this._fileSize) {
        percentComplete = (_this._bytesSoFar + bytesLoadedByCurrentRequest) / _this._fileSize;
      }
      console.log("Progress", percentComplete);
      return _this.progressStream.emit("progress", percentComplete);
    }).done();
  };

  XHRDownloadStream.prototype.cancel = function() {
    this.cancelled = true;
    this.unpipe();
    return this.currentRequestPromise.cancel();
  };

  ProgressStream = function() {};

  util.inherits(ProgressStream, events.EventEmitter);

  ProgressStream.prototype.cancel = function() {
    if (this._canceller) {
      this._canceller();
      return true;
    } else {
      return false;
    }
  };

  ProgressStream.prototype.setCanceller = function(canceller) {
    return this._canceller = canceller;
  };

  module.exports = XHRDownloadStream;

}).call(this);

/*
//@ sourceMappingURL=xhr-download-stream.map
*/
